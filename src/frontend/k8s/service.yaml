apiVersion: v1
kind: Service
metadata:
  name: trader-ai-web
  namespace: trader-ai-jjw
  labels:
    app: trader-ai-web
spec:
  type: LoadBalancer
  selector:
    app: trader-ai-web
  ports:
    - name: http
      port: 80
      targetPort: http
